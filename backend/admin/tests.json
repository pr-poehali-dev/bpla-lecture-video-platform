{
  "tests": [
    {
      "name": "Get users without auth",
      "method": "GET",
      "path": "/?action=users",
      "expectedStatus": 403,
      "expectedBody": {"error": "Доступ запрещён"},
      "bodyMatcher": "partial"
    },
    {
      "name": "Get site settings public",
      "method": "GET",
      "path": "/?action=get-settings",
      "expectedStatus": 200,
      "expectedBody": {"settings": {}},
      "bodyMatcher": "partial"
    }
  ]
}